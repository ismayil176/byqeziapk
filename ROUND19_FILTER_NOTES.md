Round 19 changes:
- Removed the Mercedes / LADA / BMW / etc. popular brand tile block from the filter popup.
- Brand and model area is now simplified to clean Marka / Model select fields only.
- Wired the advanced filter controls into the local filter engine so they now apply in-app:
  city, brand, model, condition, body type, price, currency, year, mileage,
  engine, horsepower, color, fuel, drivetrain, transmission, seats, owners,
  market, sale type, equipment, and state flags (credit, barter, no accident,
  not repainted, damaged-only, photos-only).
- Added parsing support for owners_count and sale_type from remote listings.
- Kept reset/apply behavior and filter summary updated.
